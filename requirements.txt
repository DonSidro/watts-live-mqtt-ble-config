# Upper bounds are deliberate: the release workflow installs from this file, and
# an unpinned major (bleak is already at 3.x) would otherwise land in a build
# without warning.
bleak>=0.22.0,<4
paho-mqtt>=1.6.1,<3
ttkbootstrap>=1.10.1,<3
